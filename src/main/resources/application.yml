server:
  port: 8080

spring:
  # --- Configuración para PostgreSQL (Comandos / JPA) ---
  datasource:
    url: jdbc:postgresql://postgres-db:5432/guias_db_command
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # En desarrollo, crea/actualiza el esquema automáticamente. Cambiar a 'validate' o 'none' en producción.
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # --- Configuración para MongoDB (Consultas) ---
  data:
    mongodb:
      uri: mongodb://user:password@mongo-db:27017/guias_db_query?authSource=admin