version: 1.0 
runtime: corretto17

build:
  commands:
    build:
      - mvn clean install -DskipTests
      
run:
  command: java -jar target/MicroservicioGuias-0.0.1-SNAPSHOT.jar
  network:
    port: 8080